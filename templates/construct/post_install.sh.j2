# {{ copyright }}
# {{ license }}
#
# {{ name }}-{{ variant.upper() }} {{ version }}-{{ build_number }} defaults
#
set -x

$PREFIX/bin/python - <<'____HERE'
import os, json, pathlib;
overrides = pathlib.Path(os.environ['PREFIX']) / '{{ settings_path }}/overrides.json';
overrides.parent.mkdir(parents=True, exist_ok=True);
overrides.write_text(json.dumps({{ overrides }}));
print('ok');
____HERE